[project]
name = "dataflow"
version = "0.1.0"
description = "A Python project for dataflow processing"
authors = [
  { name="Korhy", email="clementboudinel@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["dataflow"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
